[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["Dockerfile", "requirements.txt", "translate_missing.py", "railway.toml"]

[deploy]
startCommand = "python translate_missing.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
